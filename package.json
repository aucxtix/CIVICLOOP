{
  "name": "civicloop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend && npm run postinstall --prefix backend",
    "build": "npm run build --prefix frontend"
  }
}
